 STEIN CLI

## Build Setup

```bash
# install dependencies
$ npm install

# install cli
$ npm install -g .

# get 10 latest fish
$ latest-fish

# search fish with params(commodity,area,price-range): search-fish <params> <value>
$ search-fish commodity Arwana

```